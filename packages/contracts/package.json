{
  "name": "@clan-budget/contracts",
  "version": "1.0.0",
  "private": true,
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "generate:go": "mkdir -p ./gen/go && go run github.com/oapi-codegen/oapi-codegen/v2/cmd/oapi-codegen@latest -generate types,chi-server,spec -package api openapi.yaml > ./gen/go/api.gen.go",
    "generate:ts": "mkdir -p ./gen/ts && npx openapi-typescript openapi.yaml -o ./gen/ts/api.gen.ts",
    "generate": "npm run generate:go && npm run generate:ts",
    "prebuild": "npm run generate",
    "build": "tsc"
  },
  "devDependencies": {
    "openapi-typescript": "^6.7.4",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "openapi-fetch": "^0.8.2"
  }
}
